name: bq_spark_high_performance_student_portal
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  firebase_messaging: ^16.2.0
  provider: ^6.1.1
  http: ^1.2.0
  url_launcher: ^6.2.3
  cached_network_image: ^3.3.1
  flutter_local_notifications: ^16.3.0
  intl: ^0.19.0
  file_picker: ^8.0.0+1
  firebase_storage: ^13.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
